/** El middleware redirige a /marcas con sesión. Esta página es fallback de carga. */
export default function EntrarPage() {
  return (
    <div className="text-[13px] text-gray-400 mt-16 text-center">Entrando a tu espacio…</div>
  );
}
