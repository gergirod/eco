/**
 * WP-002 preview shell — hero only. Browse list arrives in WP-003.
 * Route exists for visual verification; sidebar entry is WP-005.
 */
import DiscoveryHero from "@/components/discovery/DiscoveryHero";

export default function DiscoverPreviewPage() {
  return <DiscoveryHero />;
}
